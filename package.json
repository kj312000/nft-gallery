{
  "name": "nft-gallery-root",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "postinstall": "cd frontend && npm install && npm run build && cd ../api && npm install",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd api && node index.js"
  }
}
